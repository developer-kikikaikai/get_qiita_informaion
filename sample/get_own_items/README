## 自分の記事のviews, いいね数, ストック数, コメント数を取得する。

### 使い方:
item_infos.jsonのaccess_tokenに自分のtokenを設定し、own_items.pyを実行してください。
views、ストック数、コメント数は全て別のリクエストで取得するため、記事数を少し制限しています。
maxを増やすと取得できる記事数が増えます。

### 出力:
標準出力にjson形式のviews, いいね数, ストック数, コメント数情報が出力されます。

### その他、必要環境

python3以上 (3.6で動作確認)

Ubuntuでの環境インストール方法

```
sudo apt install python3
```
